# GENERATED by harness/make_patch.py — do not edit, and do not commit.
#
# Preset: mrsx
# M + R + X + S: the full adapter, including the enforced stop gate.
#
# A preset is a per-session composition: the tools and prompt sections listed
# here are exactly what a session on this preset sees. That is what makes the
# ablation admissible, since the adapter configuration becomes the only variable.

- id: siga-memory
  name: '/Users/fanjunran/SIGA-LAMMPS/harness/siga-plugin/src/index.ts'
  config:
    memoryPath: ''
    maxChars: 8000
    activeTask: ''
    pythonBin: '/Users/fanjunran/SIGA-LAMMPS/.venv/bin/python'
    stopGate: true
    taskId: ''

- id: siga-mcp
  name: '@deepseek-ai/dsh-mcp-client'
  config:
    serverName: lammps
    transport: stdio
    command: '/Users/fanjunran/SIGA-LAMMPS/.venv/bin/python'
    args: ['-m', 'adapter.mcp_server']
    cwd: '/Users/fanjunran/SIGA-LAMMPS'
    toolCallTimeoutMs: 120000
